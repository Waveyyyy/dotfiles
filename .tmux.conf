#Quality of life stuff
set -g history-limit 10000
set -g allow-rename off

#search mode vi keys
set-window-option -g mode-keys vi

# bind keys
bind-key C send-keys " | xclip -selection clipboad"

run-shell /opt/tmux-logging/loggin.tmux
